events {}
http {
  server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/static;
    index index.html;
    
    server_name localhost;

    location / {
        include  /etc/nginx/mime.types;
	expires 365d;
        try_files $uri $uri/ =404;
    }
  }
}
